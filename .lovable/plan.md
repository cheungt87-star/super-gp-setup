

# Increase Available Staff Column Width

## Change — `src/components/rota/StaffPanel.tsx`

Update the panel width from `w-[240px]` to `w-[280px]` on the root container (line 109). This gives more room for staff names and job title badges without truncation.

